# Example environment variables for Teddy Kid Hub (Vite project)
# These are read at runtime via `import.meta.env`
# Do NOT commit a real `.env` with secrets. 
# On Factory deploys add the same keys in Project → Settings → Environment Variables.

VITE_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="YOUR_SUPABASE_ANON_KEY"
